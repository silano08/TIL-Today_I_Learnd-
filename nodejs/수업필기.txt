1. V8엔진 > 자바스크립트가 브라우저내에서만 작동가능한걸 브라우저 없이도 작동하게 만든것
node란 자바스크립트가 브라우저없이 로컬서버에서 돌아가게함

2. npm = node패키지매니저 node라이브러리

3. console.log("\n"); 줄바꿈

4. 호이스팅 - 함수안에있는 선언을 모두 끌어올려 해당함수 유효범위 최상단에 선언

5. 프로미스엔 세가지상태 : 대기 > 이행(resolve를 실행하면) > 실패(문제생기면)

6. asyne/await

await >  비동기적처리ㅏ를 동기적처리로바꿔줌

7. 라우팅 : url에 따라 다른 결과물 보여주는법
8. 미들웨어

9. 컴포넌트? 라우터?
10. 그래서 express가 뭐라고
11. 미들웨어? 운영체제와 응용 소프트웨어의 중간에서 조정과 중개역할을하는 소프트웨어
12. ejs > 템플릿엔진이라함
13. 왜 git bash인가? cmd도 있고 다른 터미널도 많은데
14. 쿼리?(query)

15. REST? REST API?

16. 스키마(schema) < 그냥 temp_html같은건가? 도식이라는데

17. 인썸니아 뭐하는프로그램?

18. Ajax , 서버에서 만든 기능을 프론트단으로 넘겨줄때
Axios 서버외부에 있는 자료를 가져오거나 주소를 호출할때

19.- SSH(Secure Shell Protocol)
    - 다른 컴퓨터에 접속할 때 쓰는 프로그램입니다. 다른 것들 보다 보안이 상대적으로 뛰어납니다.
    - 접속할 컴퓨터가 22번 포트가 열려있어야 접속 가능합니다. AWS EC2의 경우, 이미 22번 포트가 열려있습니다. 확인해볼까요?

20. 리눅스 명령어
ls: 내 위치의 모든 파일을 보여준다.

pwd: 내 위치(폴더의 경로)를 알려준다.

mkdir: 내 위치 아래에 폴더를 하나 만든다.

cd [갈 곳]: 나를 [갈 곳] 폴더로 이동시킨다.

cd .. : 나를 상위 폴더로 이동시킨다.

cp -r [복사할 것] [붙여넣기 할 것]: 복사 붙여넣기

rm -rf [지울 것]: 지우기

sudo [실행 할 명령어]: 명령어를 관리자 권한으로 실행한다.
sudo su: 관리가 권한으로 들어간다. (나올때는 exit으로 나옴)